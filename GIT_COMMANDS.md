# Git Commands

```bash
git init
git add .
git commit -m "Initial release of Smart Water Quality System"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/smart-water-quality-system.git
git push -u origin main
```
